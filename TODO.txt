(  ) Expand session functionalities to XQuery.
	(OK) Rename the namespace to session
	(OK) List possible functions
		(OK) getCreationTime
		(OK) getLastAccessedTime
		(OK) setMaxInactiveInterval
		(OK) getMaxInactiveInterval
		(OK) getAttribute (getAtt)
		(OK) getAttributeNames
		(OK) setAttribute (setAtt)
		(OK) removeAttribute (rmAtt)
		(OK) invalidate
		(OK) clear
	(OK) Implement these functions 
	(OK) create signatures
	(OK) Unit Test these functions
		(OK) Create structure for these testings
		(OK) Better test the functions. Do more elaborated tests.
(OK) Define Applications skeleton (with and without MVC)
	(  ) Support applications without MVC new skeleton
	(  ) SUpport applications with new MVC Skeleton
(  ) Create application for creation of applications
	Simple for skeleton creation
(  ) Create application for applications checking
	Queries information
	Naming
	Usage
	Clicking information?
(  ) Create documentation application
(  ) Create mechanism for hot-deployment of applications
(  ) Create authentication screen for private content
(  ) Create mapping between model->view->controller
	(OK)One model to one controller to many views. 
	(  )Waiting for import modules implementation 
(OK) HttpSessionQueryContext should extend TXQueryContext
	To match and solve Roxana's problems.
(OK) Automatically store all files for all applications (AppServlet)
	(OK) Problems with locking mechanism. Test it more and explain to Sebastian.
	(  ) Use such blob files directly from disk. Better for performance.
(  ) Create new app servlet structure
	(OK) Create support for resources
	(OK) Create support for singular query execution
	(OK) Create support for MVC query execution
	(  ) Optimize such structure
(  ) Migrate old applications to new structure
	(  ) eCommerce (MVC)
	(OK) eCommerce (non-MVC)
	(  ) query engine (non MVC)
	(  ) Error application (Servlet)
	(  ) Create one Collection for each application and use it, instead of the folders!
		(OK) Adapt store file and delete file functions. Receive collection name, or not
		(OK) Test adapted functions
		(  ) Unit test adapted functions
			(  ) AddDocToCollection
			(  ) CreateCollection
			(  ) DropCollection
			(  ) StoreDoc